const Banner = () => {

  return (
    <div>

      <div class="w-80 h-10 bg-gradient-to-r from-primary to-secondary rounded-lg opacity-50 -z-1 top-0 bottom-0 -left-50 -right-50 fixed"></div>
      <div class="w-80 h-10 bg-gradient-to-r from-primary to-secondary rounded-lg opacity-50 -z-1 top-0 bottom-0 -left-50 -right-50 fixed transition duration-4000 "></div>
      <div class="w-80 h-10 bg-gradient-to-r from-primary to-secondary rounded-lg opacity-50 -z-1 top-0 bottom-0 -left-50 -right-50 fixed transition duration-4000 "></div>

    </div>
  )
}

export default Banner;